# Server
PORT=5001
NODE_ENV=development
CLIENT_ORIGIN=http://localhost:3000

## Mongo (Choose ONE) -------------------------------------------------
# Remote Atlas connection string. Include your Atlas user/pass and cluster host.
# NOTE: If your password contains special characters (@ : / ? # etc) URL-encode them.
DATABASE_URL=mongodb+srv://<username>:<password>@<cluster-host>/?retryWrites=true&w=majority&appName=<app-name>
# The logical database inside the cluster where application collections will be stored.
DATABASE_NAME=miko

# JWT
JWT_SECRET=replace_me_dev_secret
JWT_EXPIRES=1d

# Google OAuth
GOOGLE_CLIENT_ID=YOUR_GOOGLE_CLIENT_ID

GOOGLE_CLIENT_SECRET=YOUR_GOOGLE_CLIENT_SECRET

# Email (Resend)
# Obtain API key from https://resend.com -> API Keys (Full Access for dev). 
# EMAIL_FROM must use a verified sending domain (e.g., noreply@miko.app).
RESEND_API_KEY=YOUR_RESEND_API_KEY
EMAIL_FROM=your_verified_sender@example.com

# OTP
OTP_LENGTH=6
OTP_TTL_MS=300000
# Optional dev helpers (DO NOT enable in production)
# LOG_OTP_CODES=true          # Logs every generated OTP to server console
# OTP_DEV_EXPOSE=true          # Adds { devCode } to /otp/request response for local testing

# Wallet (message salt / domain)
WALLET_LOGIN_DOMAIN=miko.app
WALLET_MESSAGE_PREFIX=Miko Auth

# Mongo Express env removed—manage data via MongoDB Atlas UI or Compass.
